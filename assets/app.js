const $=s=>document.querySelector(s);const visibleTabs=new Set(['board','paper','trades','performance','factor','model','review']);let tab=visibleTabs.has(location.hash.slice(1))?location.hash.slice(1):'board',D={};
const pct=x=>x==null?'—':(100*Number(x)).toFixed(1)+'%';
const money=x=>x==null?'—':'$'+Number(x).toFixed(2);
async function j(path,def={}){try{return await fetch(path+'?'+Date.now()).then(r=>r.ok?r.json():def)}catch(e){return def}}
async function load(){for(const x of ['board','trades','performance','config','approval','sportsbook_consensus','model_fair','football_intelligence','backtest','calibrated_params','clv','alpha_attribution'])D[x]=await j('data/'+x+'.json',{});D.factor_attribution=await j('data/reviews/2026-09-19-morning_factor_attribution.json',{});D.legacy_morning=await j('paper/legacy_morning_whatif_2026-09-19.json',{});D.paper_state=await j('paper/state.json',{});D.paper_account=await j('paper/account.json',{});D.paper_observations=await j('paper/observations.json',{});D.paper_archive=await j('paper/research_archive.json',{});D.morning_review=await j('data/reviews/2026-09-19-morning.json',{});D.afternoon_review=await j('data/reviews/2026-09-19-afternoon-research.json',{});D.pit_latest=await j('data/pit/latest.json',{items:[]});D.pit_closing=await j('data/pit/closing.json',{items:[]});$('#updated').textContent='最后更新 / Updated: '+(D.board.updated||D.pit_latest.updated_utc||'—');render()}
function cards(items){return '<div class="cards">'+items.map(([a,b,c=''])=>'<div class="card"><span class="muted">'+a+'</span><div class="metric '+c+'">'+b+'</div></div>').join('')+'</div>'}
function groupTable(title,rows){return '<div class="panel"><h2>'+title+'</h2><table><tr><th>Group</th><th>N</th><th>Closed</th><th>ROI</th><th>CLV N</th><th>Mean CLV</th></tr>'+((rows||[]).map(x=>'<tr><td>'+x.group+'</td><td>'+x.n+'</td><td>'+x.closed_n+'</td><td class="'+(x.roi>0?'positive':x.roi<0?'negative':'')+'">'+pct(x.roi)+'</td><td>'+x.clv_n+'</td><td class="'+(x.mean_clv_pp>0?'positive':x.mean_clv_pp<0?'negative':'')+'">'+pct(x.mean_clv_pp)+'</td></tr>').join(''))+'</table></div>'}

const fmtUSD=x=>x==null?'—':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(x));
const safe=x=>String(x??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmtTime=x=>x?new Date(x).toLocaleString('en-US',{timeZone:'America/New_York',month:'short',day:'numeric',hour:'numeric',minute:'2-digit',timeZoneName:'short'}):'—';
function renderPaper(){const a=D.paper_account||{},st=D.paper_state||{},w=D.legacy_morning||{},sum=w.summary||{},obs=D.paper_observations.observations||[],fills=st.trades||[];let h='<div class="panel"><h2>Paper Trading · 虚拟资金账户</h2><p>Independent virtual ledger / 独立模拟账本 · Paper only · no automatic real-money execution</p><div class="notice">真实历史下注、赛前假设收益和正式 paper fills 是三套不同账本。没有真实记录的盘口/时间戳/成交，不能回填成 paper profit。</div></div>';h+=cards([['Paper NAV',fmtUSD(st.realized_nav_usd??a.initial_capital_usd)],['Available Cash',fmtUSD(st.available_cash_usd??a.starting_cash_usd)],['Paper Realized P&L',fmtUSD(st.realized_pnl_usd??0),(st.realized_pnl_usd??0)>=0?'positive':'negative'],['Open Exposure',fmtUSD(st.open_stake_usd??0)],['Confirmed Paper Orders',st.paper_orders??fills.length],['Unit / 1u',fmtUSD(a.unit_usd??5000)]]);h+='<div class="panel"><h2>Paper orders / 模拟成交记录</h2><p class="muted">Settlement comes only from the paper ledger. No research idea is auto-booked.</p><div class="table-scroll"><table><tr><th>Match / Position</th><th>Entry</th><th>Stake</th><th>Status</th><th>Realized P&L</th></tr>'+ (fills.length?fills.map(t=>'<tr><td>'+safe(t.match||t.position||t.pick_id)+'</td><td>'+safe(t.decimal_odds||t.entry_price)+'</td><td>'+fmtUSD(t.stake_usd)+'</td><td>'+safe(t.status)+'</td><td>'+fmtUSD(t.pnl_usd)+'</td></tr>').join(''):'<tr><td colspan="5" class="muted">No confirmed paper fills yet / 尚无已确认模拟成交</td></tr>')+'</table></div></div>';h+='<div class="panel"><h2>Sept 19 morning desk · What-if (NOT paper NAV)</h2><p class="muted">Historical assistant-reported T-1H picks; no independently verified executable fills or slippage/fees.</p>'+cards([['Research Stake',fmtUSD(sum.settled_stake_usd)],['What-if P&L',fmtUSD(sum.net_pnl_usd),'positive'],['What-if ROI',pct(sum.roi_on_stake)],['What-if NAV',fmtUSD(sum.what_if_nav_usd)]])+'<div class="table-scroll"><table><tr><th>Match</th><th>Market</th><th>Odds</th><th>My Fair</th><th>Grade</th><th>Stake</th><th>Result</th><th>What-if P&L</th></tr>'+(w.trades||[]).map(t=>'<tr><td>'+safe(t.match)+'</td><td>'+safe(t.market)+'</td><td>'+safe(t.odds)+'</td><td>'+pct(t.fair)+'</td><td>'+safe(t.grade)+'</td><td>'+fmtUSD(t.stake_usd)+'</td><td>'+safe(t.result)+'</td><td class="positive">'+fmtUSD(t.pnl_usd)+'</td></tr>').join('')+'</table></div></div>';h+='<div class="panel"><h2>Unpriced / No-entry research · 非成交研究</h2><div class="table-scroll"><table><tr><th>Match</th><th>Market</th><th>Decision</th><th>Why / Status</th></tr>'+(w.excluded_non_trades||[]).map(t=>'<tr><td>'+safe(t.match)+'</td><td>'+safe(t.market)+'</td><td>'+safe(t.decision)+'</td><td>'+safe(t.reason)+'</td></tr>').join('')+obs.map(t=>'<tr><td>'+safe(t.match)+'</td><td>'+safe(t.market)+'</td><td>'+safe(t.decision)+'</td><td>'+safe(t.status)+'</td></tr>').join('')+'</table></div></div>';h+='<div class="panel"><h2>Risk budget / 资金风控</h2><p>Per match ≤ '+fmtUSD(a.maximum_exposure_per_match_usd)+' · Total open ≤ '+fmtUSD(a.maximum_total_open_exposure_usd)+' · A single 0.5–0.75u · B two-leg 0.25–0.5u · C speculative ≤0.25u.</p><p class="muted">No watch-only holding period: qualifying risk/reward signals enter the virtual account when the scheduled paper processor receives a valid frozen signal. In-play entries still need synchronized score/cards/clock, independently priced conditional fair and a timestamped quote.</p></div>';return h}
function renderReview(){const r=D.morning_review||{},af=D.afternoon_review||{},w=D.legacy_morning||{};let h='<div class="panel"><h2>September 19 · Football Intelligence Review / 今日模型复盘</h2><p class="muted">Morning desk audit + afternoon research watchlist. This is not a calibrated backtest or an executed-trade ledger.</p><div class="notice">Today’s outcomes must not be used to reconstruct a pre-match probability, fabricate a fill, or fit coefficients to a single winning match. PASS is not a winning or losing trade.</div></div>';h+=cards([['Morning What-if Wins',(w.summary||{}).wins??'—'],['Morning What-if P&L',fmtUSD((w.summary||{}).net_pnl_usd)],['Confirmed Paper P&L',fmtUSD((D.paper_state||{}).realized_pnl_usd??0)],['Afternoon Research Ideas',(af.watch||[]).length],['Production Model Fair Rows',(D.model_fair.items||[]).length],['Live Quote Verification',af.confirmed_repo_before_scan?.sportsbook_consensus_status||'—']]);h+='<div class="panel"><h2>Bundesliga · Full fixture review</h2><div class="table-scroll"><table><tr><th>Match</th><th>FT</th><th>Morning decision</th><th>Learning / 模型修正方向</th></tr>'+(r.bundesliga_matchday_review||[]).map(t=>'<tr><td>'+safe(t.match)+'</td><td>'+safe(t.result||t.status)+'</td><td>'+safe(t.morning_desk_call)+'</td><td>'+safe(t.learning)+'</td></tr>').join('')+'</table></div></div>';h+='<div class="panel"><h2>Other morning candidates / 其他场次</h2><div class="table-scroll"><table><tr><th>Match</th><th>Market</th><th>Decision</th><th>Score / State</th><th>Process review</th></tr>'+(r.matched_recommendations||[]).map(t=>'<tr><td>'+safe(t.match)+'</td><td>'+safe(t.market)+'</td><td>'+safe(t.original_desk_decision)+'</td><td>'+safe(t.final_score||t.result_status)+'</td><td>'+safe(t.process_review||'Not settled / no fill')+'</td></tr>').join('')+'</table></div></div>';h+='<div class="panel"><h2>Afternoon market research / 下午市场研究</h2><p class="muted">Public odds samples below are not current executable quotes; Research rows are not paper orders without valid frozen price, independent fair and stake.</p><div class="table-scroll"><table><tr><th>Match</th><th>League</th><th>Kickoff ET</th><th>Research quote / context</th><th>Decision</th></tr>'+(af.watch||[]).map(t=>'<tr><td>'+safe(t.match)+'</td><td>'+safe(t.competition)+'</td><td>'+fmtTime(t.kickoff_utc)+'</td><td>'+safe(t.research_price_sample||t.injury_context||'—')+'</td><td>'+safe(t.decision)+'</td></tr>').join('')+'</table></div></div>';h+='<div class="panel"><h2>Model learning backlog / 复盘规则</h2><div class="learning-grid">'+(r.learning_rules||[]).map((t,i)=>'<div class="learning-item"><b>'+String(i+1).padStart(2,'0')+'</b><span>'+safe(t)+'</span></div>').join('')+'</div></div>';return h}
function render(){document.querySelectorAll('nav button').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));let h='';const picks=D.board.picks||[];
if(tab==='board'){h=cards([['候选 / Ideas',picks.length],['未平仓 / Open',money(D.performance.open_stake_usd)],['已实现 P&L / Realized',money(D.performance.realized_pnl_usd),D.performance.realized_pnl_usd>=0?'positive':'negative'],['已平仓 ROI / Closed ROI',pct(D.performance.closed_roi),D.performance.closed_roi>=0?'positive':'negative']]);h+='<div class="panel"><table><tr><th>比赛 / Match</th><th>市场 / Market</th><th>Price</th><th>Fair</th><th>Book</th><th>Model-Book</th><th>Edge</th><th>EV</th><th>Grade</th><th>Bucket</th><th>Core?</th><th>Max</th></tr>'+picks.map(x=>{const c=(D.sportsbook_consensus.items||[]).find(z=>z.pick_id===x.id)||{};return '<tr><td>'+x.match+'<br><span class="muted">'+x.competition+'</span></td><td>'+x.market+'</td><td>'+x.decimal_odds+'</td><td>'+pct(x.fair_probability)+'</td><td>'+pct(c.consensus_probability)+'</td><td>'+pct(c.model_vs_consensus_pp)+'</td><td>'+pct(x.edge_pp)+'</td><td class="'+(x.ev>=0?'positive':'negative')+'">'+pct(x.ev)+'</td><td class="'+x.grade+'">'+(x.grade||'—')+'</td><td>'+(x.bucket||x.portfolio_bucket||'—')+'</td><td>'+(x.core_parlay_eligible?'YES':'NO')+'</td><td>'+(x.max_units!=null?x.max_units+'u':'—')+'</td></tr>'}).join('')+'</table></div>'}
if(tab==='paper')h=renderPaper();
if(tab==='review')h=renderReview();
if(tab==='approval')h='<div class="panel"><h2>人工审批 / Approval Queue</h2><p class="muted">Paper signals only; no automatic real-money execution.</p><table><tr><th>Signal</th><th>Grade</th><th>Size</th><th>Max Entry</th><th>Kalshi</th><th>Status</th></tr>'+(D.approval.items||[]).map(x=>'<tr><td>'+x.signal+'</td><td class="'+x.grade+'">'+x.grade+'</td><td>'+x.suggested_units+'u</td><td>'+x.max_entry+'</td><td>'+(x.kalshi_ticker||'unmapped')+'</td><td>'+x.status+'</td></tr>').join('')+'</table></div>';
if(tab==='trades')h='<div class="panel"><h2>Historical user trades / 真实历史交易（非 Paper）</h2><p class="muted">These records are separate from the $1m paper account. Closed positions with missing final payout remain P&L pending.</p></div><div class="panel"><table><tr><th>Date</th><th>Position</th><th>Stake</th><th>Payout/Exit</th><th>P&L</th><th>Status</th><th>Execution</th></tr>'+(D.trades.trades||[]).map(x=>'<tr><td>'+x.date+'</td><td>'+x.position+'</td><td>'+money(x.stake_usd)+'</td><td>'+x.payout_display+'</td><td class="'+(x.pnl_usd>0?'positive':x.pnl_usd<0?'negative':'')+'">'+(x.pnl_usd==null?'OPEN':money(x.pnl_usd))+'</td><td>'+x.status+'</td><td>'+x.execution+'</td></tr>').join('')+'</table></div>';
if(tab==='performance'){let p=D.performance||{};h='<div class="panel"><h2>Performance · Separate ledgers / 分账业绩</h2><p class="muted">Historical user positions ≠ virtual paper fills ≠ research what-if. Historical ROI uses only settled positions with known P&L.</p></div>'+cards([['Paper NAV',fmtUSD(D.paper_state.realized_nav_usd??1000000)],['Paper P&L',fmtUSD(D.paper_state.realized_pnl_usd??0)],['Morning What-if',fmtUSD(D.legacy_morning.summary?.net_pnl_usd)],['Historical Realized',fmtUSD(p.realized_pnl_usd),p.realized_pnl_usd>=0?'positive':'negative']]);h+=cards([['Closed Stake',money(p.closed_stake_usd)],['Realized P&L',money(p.realized_pnl_usd),p.realized_pnl_usd>=0?'positive':'negative'],['ROI',pct(p.closed_roi),p.closed_roi>=0?'positive':'negative'],['Open Stake',money(p.open_stake_usd)],['Trades',p.trade_count]]);h+='<div class="panel"><h2>Research Metrics</h2><p>CLV: '+(p.clv||'tracking prospectively')+'</p><p>Brier Score: '+(p.brier_score||'—')+'</p><p>EV Realization: '+(p.ev_realization||'—')+'</p></div>'}
if(tab==='model'){let b=D.backtest||{},cp=D.calibrated_params||{};h='<div class="panel"><h2>Model Lab · Fair Pricing & Validation</h2><p class="muted">Independent fair should come from football features, not sportsbook prices. No retrospective factor coefficients.</p><div class="notice">Model health: '+safe(D.football_intelligence.status||'unknown')+' · Sportsbook consensus: '+safe(D.sportsbook_consensus.status||'unknown')+'. If provider feeds are unavailable, empty fair rows are a data-availability limitation, not a zero-probability forecast.</div></div>'+cards([['PIT Snapshots',(D.pit_latest.items||[]).length],['Closing References',(D.pit_closing.items||[]).length],['Tracked CLV',D.clv.summary?.tracked_legs??D.clv.summary?.tracked_trades??0]]);h+=cards([['Backtest Status',b.status||'—'],['Holdout N',b.holdout?.n??'—'],['Holdout Log Loss',b.holdout?.log_loss??'—'],['Holdout Brier',b.holdout?.brier??'—'],['Calibration Active',cp.active?'YES':'NO']]);h+='<div class="panel"><h2>My Fair Probability / 模型拆解</h2><p class="muted">Sportsbook/Kalshi prices are excluded from My Fair.</p><table><tr><th>Pick</th><th>λ Home</th><th>λ Away</th><th>1</th><th>X</th><th>2</th><th>Fair</th><th>Status</th></tr>'+(D.model_fair.items||[]).map(x=>'<tr><td>'+x.pick_id+'</td><td>'+(x.lambda_home??'—')+'</td><td>'+(x.lambda_away??'—')+'</td><td>'+pct(x.home_win)+'</td><td>'+pct(x.draw)+'</td><td>'+pct(x.away_win)+'</td><td>'+pct(x.fair_probability)+'</td><td>'+x.status+'</td></tr>').join('')+'</table></div><div class="panel"><h2>Pricing research framework / 今天的模型分析</h2><div class="learning-grid"><div class="learning-item"><b>01</b><span>Attack/defense strength + xG, xG/shot, shot volume and opponent-adjusted recent form; separate finishing variance from chance creation.</span></div><div class="learning-item"><b>02</b><span>Coach/style matchup, transitions, pressing/build-up risk, squad depth, confirmed XI, injury, rest and motivation. Record human-vs-model disagreement prospectively.</span></div><div class="learning-item"><b>03</b><span>Expected goals λ_home/λ_away → Poisson score matrix → 1X2, totals, BTTS, handicap. Assess asymmetric score distributions and game-state paths.</span></div><div class="learning-item"><b>04</b><span>Model fair probability vs timestamped de-vig consensus/market ask → edge, EV and risk gate; market price is benchmark, not a football factor.</span></div><div class="learning-item"><b>05</b><span>Chronological point-in-time snapshots, holdout calibration, CLV and Brier/log-loss; no leakage from final scores into historical pricing.</span></div><div class="learning-item"><b>06</b><span>Live conditional model requires synchronized score, minute, cards, XI, subs and fresh executable orderbook; paper entry only after approval and exposure checks.</span></div></div></div>'}
if(tab==='pit'){const s=D.clv.summary||{},latest=D.pit_latest.items||[],closes=D.pit_closing.items||[];h=cards([['Hourly snapshots',latest.length],['Closing refs',closes.length],['CLV legs tracked',s.tracked_legs??s.tracked_trades??0],['Mean Trade CLV',pct(s.mean_trade_clv_pp)]]);h+='<div class="panel"><h2>Point-in-Time / 时点快照</h2><table><tr><th>Pick</th><th>Bucket</th><th>Minutes</th><th>My Fair</th><th>Book</th><th>Kalshi</th><th>Liquidity</th></tr>'+latest.map(x=>'<tr><td>'+x.pick_id+'</td><td>'+x.bucket+'</td><td>'+(x.minutes_to_kickoff??'—')+'</td><td>'+pct(x.my_fair)+'</td><td>'+pct(x.sportsbook_probability)+'</td><td>'+pct(x.kalshi_probability)+'</td><td>'+money(x.kalshi_liquidity_usd)+'</td></tr>').join('')+'</table></div>'}
if(tab==='alpha'){const a=D.alpha_attribution||{},s=a.summary||{};h=cards([['Status',a.status||'—'],['Rows',s.rows??0],['Closed Rows',s.closed_rows??0],['Tracked CLV',s.tracked_clv_rows??0],['Realized ROI',pct(s.realized_roi),s.realized_roi>0?'positive':s.realized_roi<0?'negative':''],['Mean CLV',pct(s.mean_clv_pp),s.mean_clv_pp>0?'positive':s.mean_clv_pp<0?'negative':'']]);h+=groupTable('按联赛 / By League',a.by_league)+groupTable('按市场 / By Market Type',a.by_market_type)+groupTable('按等级 / By Grade',a.by_grade)+groupTable('按入场时点 / By Entry Horizon',a.by_entry_horizon)+groupTable('按Edge Bucket',a.by_edge_bucket);h+='<div class="panel"><p class="muted">UNKNOWN means historical metadata was not available. No retroactive CLV is fabricated.</p></div>'}
if(tab==='factor'){const a=D.factor_attribution||{},sm=a.summary||{},ts=a.trades||[],ledger=D.legacy_morning||{};const usd=x=>x==null?'—':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(x));const p2=x=>x==null?'—':(100*Number(x)).toFixed(2)+'%';const e=x=>x==null?'—':Number(x).toFixed(3);h='<div class="panel"><h2>2026-09-19 Morning What-if / 逐场收益及因子拆解</h2><p class="muted">08:00–10:00 ET morning cohort · archived T-1H quoted prices · research simulation only, NOT confirmed executable fills.</p><div class="notice">IMPORTANT / 重要：下方逐因子的 probability contribution 与 P&L 分摊是事后示意性 allocation，缺少赛前冻结的逐因子模型输出；不能当作已验证的真实 factor alpha、实际因子赔率或因果归因。真正可核对的是每场整单 odds / fair / stake / result / what-if P&L。Market price gap 是定价差，不是独立足球模型因子。</div></div>';h+=cards([['What-if Net P&L',usd(sm.realized_whatif_pnl_usd),'positive'],['Total Stake',usd(sm.settled_stake_usd)],['ROI on Stake',p2(sm.roi_on_stake),'positive'],['Ex-ante Model EV',usd(sm.ex_ante_model_ev_usd)],['What-if NAV',usd(sm.whatif_nav_usd)],['Actual Paper NAV',usd((D.paper_state||{}).realized_nav_usd??sm.actual_paper_nav_usd)]]);h+='<div class="panel"><h2>Trade-level settlement / 每场结算</h2><div class="table-scroll"><table><tr><th>Match</th><th>Market</th><th>Entry Odds</th><th>Break-even</th><th>My Fair</th><th>Fair Odds</th><th>Edge</th><th>Stake</th><th>Ex-ante EV $</th><th>Result</th><th>What-if P&L</th></tr>'+ts.map(t=>'<tr><td>'+t.short_match+'</td><td>'+t.market+'</td><td>'+e(t.decimal_odds)+'</td><td>'+p2(t.market_break_even_probability)+'</td><td>'+p2(t.model_fair_probability)+'</td><td>'+e(1/t.model_fair_probability)+'</td><td>'+p2(t.total_edge_pp)+'</td><td>'+usd(t.stake_usd)+'</td><td>'+usd(t.ex_ante_model_ev_usd)+'</td><td>'+t.result+'</td><td class="positive">'+usd(t.realized_whatif_pnl_usd)+'</td></tr>').join('')+'</table></div></div>';for(const t of ts){const fs=t.factor_rows||[],sum=fs.reduce((n,r)=>n+Number(r.realized_pnl_factor_attribution_usd||0),0);h+='<div class="panel"><h2>'+t.short_match+' — factor waterfall / 因子瀑布</h2><p class="muted">Illustrative allocation; ordered cumulative fair odds depend on arbitrary factor order. Baseline is market break-even, NOT an independently estimated model baseline.</p><div class="table-scroll"><table><tr><th>Illustrative factor</th><th>Δ probability (pp)</th><th>Cumulative fair</th><th>Cumulative fair odds</th><th>Illustrative EV $</th><th>Allocated realized P&L $</th></tr>'+fs.map(r=>'<tr><td>'+r.factor+'</td><td>'+p2(r.probability_contribution_pp)+'</td><td>'+p2(r.cumulative_fair_after_factor)+'</td><td>'+e(r.cumulative_fair_odds_after_factor)+'</td><td>'+usd(r.factor_ev_usd)+'</td><td>'+usd(r.realized_pnl_factor_attribution_usd)+'</td></tr>').join('')+'</table></div><p class="muted">Displayed factor allocation sum: '+usd(sum)+'; trade what-if P&L: '+usd(t.realized_whatif_pnl_usd)+'; any cent difference is rounding, not another trade.</p></div>'}h+='<div class="panel"><h2>Excluded from P&L / 非成交信号</h2><p class="muted">Arsenal ML: PASS in reconciled morning review; Hamburg–Köln Over 2.5: negative-EV PASS; Mainz ML: unpriced lean. Missing original stake/odds must not be invented. Any recovered timestamped original recommendation requires an append-only correction.</p><p class="muted">Actual verified paper fills: '+(sm.actual_verified_fills??0)+' · Actual paper P&L: '+usd((D.paper_state||{}).realized_pnl_usd??0)+' · no fees or slippage in quoted what-if.</p></div>'}
if(tab==='risk')h='<div class="panel"><h2>Portfolio 风控规则 / Risk Rules</h2><p><b>A</b> genuine-edge single → 0.5–0.75u</p><p><b>B / Core Parlay</b> 2 legs only → probability first, then EV; market ≥65%, My Fair ≥68%, edge ≥3pp, combined My Fair ≥50%; different matches required, different competitions preferred → 0.25–0.5u</p><p><b>C</b> 2.5x–4x speculative combo → ≤0.25u</p><p>Correlated exposure per match → ≤1u</p><p>General gate: Edge ≥4pp, EV ≥5%, liquidity/max-entry checks.</p><p><b>58% rule:</b> a leg below 65% market probability or 68% My Fair cannot be labeled Core/稳腿; it may only be Value Single / speculative depending on edge.</p></div>';
$('#content').innerHTML=h}
document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;location.hash=tab;render()});load();
